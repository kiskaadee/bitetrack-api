[project]
name = "bitetrack-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.138.2",
    "pytest>=9.1.1",
]

[dependency-groups]
dev = [
    "pyright>=1.1.411",
]
